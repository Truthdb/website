name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  ci:
    uses: Truthdb/.github/.github/workflows/node-ci.yml@issue-17-workflows
